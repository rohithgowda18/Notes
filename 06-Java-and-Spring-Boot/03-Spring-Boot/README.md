# 🍃 Spring Boot — Complete Framework Notes

This folder contains end-to-end revision material for the Spring Framework and Spring Boot enterprise ecosystem.

---

## 📑 Contents

- 📄 **[Spring Boot Complete Notes.pdf](./Spring%20Boot%20Complete%20Notes.pdf)**:
  - Spring Core: IoC Container, Dependency Injection types (Constructor vs Setter vs Field), Bean Scopes & Lifecycle
  - Spring Boot Auto-configuration & Starters (`spring-boot-starter-web`, `spring-boot-starter-data-jpa`, `spring-boot-starter-security`)
  - Spring MVC: Request mapping, Controllers vs RestControllers, Exception Handling (`@ControllerAdvice`, `@ExceptionHandler`), DTO pattern & Validation (`@Valid`)
  - Spring Data JPA: Hibernate ORM, EntityManager, Repositories (`JpaRepository`), Derived Query Methods, `@Query`, `@Transactional` propagation and isolation
  - Spring Security: Filter Chains, Authentication Providers, JWT Token generation & validation, Role-based Access Control (RBAC)
  - Actuator, Health Checks, Metrics, and Production Deployment.
